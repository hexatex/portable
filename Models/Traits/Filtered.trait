use Example/Dtos/Filters

Filtered
    scopes
        filter(Filters filters) {
            foreach string column, where of filters.wheres {
                query.where(column, 'like', where)
            }
        }
